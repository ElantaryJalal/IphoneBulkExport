# iPhone Bulk Exporter (iphone_export.py / iphone_export_gui.py).
# AFC path needs the Apple Mobile Device USB driver (iTunes / Apple Devices app);
# the GUI falls back to MTP (pywin32, Windows-only) when that driver is absent.
pymobiledevice3>=2.30.0
tqdm>=4.66.0
pywin32>=306; sys_platform == "win32"

# Optional — only for HEIC->JPG conversion (--jpg):
# pillow>=10.0.0
# pillow-heif>=0.13.0
